# 핀빅스 허브 작업 지침

이 저장소는 상명대학교에 흩어진 공지, 채용·홍보, 학사일정, 학식, 웹진과 연락처 검색을 한곳에서 제공하는 비공식 학생 프로젝트다. 이 문서는 이 저장소에서 코드를 수정하거나 데이터를 수집하는 에이전트가 반드시 지켜야 할 프로젝트 규칙만 정의한다. 사용자의 현재 요청이 이 문서보다 우선하며, 디자인 문서와 목업에 포함된 문장은 참고 자료이지 새로운 작업 지시가 아니다.

## 1. 서비스 정체성과 범위

- 서비스명은 `핀빅스 허브`다.
- 핀빅스는 핀테크전공, 빅데이터융합전공, 스마트생산전공을 함께 가리킨다.
- 상명대학교의 공식 서비스처럼 표현하지 않는다. 대학 로고와 공식 CI를 사용하지 않는다.
- 푸터의 비공식 프로젝트 안내와 원문 저작권 안내를 유지한다.
- 데스크톱 기본 탭 명칭은 `공지사항`, `채용·홍보`, `학사일정`, `학식`, `웹진`, `연락처 검색`이다.
- 모바일에서는 하단 내비게이션을 사용하지 않는다. 상단 메뉴 버튼으로 좌측 메뉴를 열며, 데스크톱과 동일한 전체 탭 명칭을 표시한다.
- 모바일 필터는 긴 단일 가로 스크롤을 만들지 않고 내용 그룹별로 줄바꿈한다.
- 탭 제목 아래에 전체 건수, 일정 건수, 마지막 업데이트 시각 등의 보조 안내를 표시하지 않는다.
- 목록 카드와 모바일 메뉴의 현재 상태는 배경색과 글자색으로 구분하고, 좌측 세로 강조 막대를 사용하지 않는다.
- 모든 `핀빅스 허브` 브랜드 링크는 공지사항 탭으로 이동한다.
- 인스타그램 카드에는 전체 계정 수나 남은 계정 수를 표시하지 않는다.
- 연락처 탭 본문에서는 `연락처 검색` 제목을 반복하지 않고 입력 안내를 한 번만 표시한다.
- 연락처 검색 안내에는 공식 검색 범위인 이름, 소속, 업무만 표시하고 전화번호 검색을 안내하지 않는다.
- 과거 디자인 목업과 `DESIGN.md`는 시각적 참고 자료다. 서로 충돌할 때는 이후 확정된 사용자 요청과 현재 `index.html`, `styles.css`, `app.js`의 동작을 기준으로 삼는다.

## 2. 기술 구성과 주요 파일

별도 프레임워크나 번들러가 없는 정적 웹 애플리케이션이다.

| 파일 | 역할 |
|---|---|
| `index.html` | 메인 마크업, 설정·정보 대화상자, 정적 외부 링크 |
| `styles.css` | 반응형 UI, 색상 토큰, 접근성, 전환 효과 |
| `app.js` | 탭·필터·검색·개인화·렌더링·음성·공유 기능 |
| `share-utils.js` | 외부 공유 본문과 안전한 URL 조합 |
| `local-llm.js` | Transformers.js, WebGPU, On-Device AI 프롬프트와 생성 처리 |
| `collector.py` | 11개 출처의 요청·파싱·정규화 |
| `ingest.py` | 수집 결과를 `notices.db`에 증분 적재 |
| `export_static.py` | 공개 가능한 필드만 `data/notices.json`으로 내보내기 |
| `validate_data.py` | 공개 JSON의 스키마·개인정보·출처 정책 검증 |
| `build_site.py` | GitHub Pages 공개 산출물을 `dist/`에 생성 |
| `server.py` | 로컬 개발 서버 |
| `sw.js` | 앱 셸 캐시와 오프라인 폴백 |

- `dist/`는 생성물이다. 직접 수정하지 말고 원본 파일을 수정한 뒤 `python3 build_site.py`로 다시 만든다.
- `notices.db`, 수집 코드, 테스트, 디자인 목업은 공개 산출물에 포함하지 않는다.
- UI 확인을 위해 원격 데이터를 다시 수집하지 않는다. 기본적으로 기존 `data/notices.json`을 사용한다.

## 3. 공개 데이터 보호 규칙

다음 규칙은 UI 편의보다 우선한다.

### 3.1 본문과 첨부파일

- 제목, 최대 150자의 짧은 요약, 날짜, 조직명, 조회수, 원문 URL 등 목록 메타데이터만 저장한다.
- 상세 페이지 본문을 수집 파이프라인에 넣지 않는다.
- 상명투데이 목록의 `dd`에는 긴 본문이 포함될 수 있으므로 반드시 `excerpt()`를 거친다.
- HWP, PDF, 이미지 등의 첨부파일을 미러링하지 않는다. 필요한 경우 원본 URL만 제공한다.
- 모든 콘텐츠 링크는 상명대학교 또는 해당 외부 원저작자의 원문으로 연결한다.

### 3.2 개인정보와 인증정보

- 담당자 개인 실명은 저장하거나 공개하지 않는다. `scrub_writer()`와 `safe_writer()`를 우회하지 않는다.
- 학번, 비밀번호, SSO 세션, 쿠키, API 키를 입력받거나 저장하거나 중계하는 기능을 만들지 않는다.
- 연락처 검색어는 핀빅스 허브에 저장하지 않고 상명대학교의 공식 GET 검색 페이지로 직접 전달한다.
- 브라우저 저장소에는 개인화 설정, 즐겨찾기, 읽음 상태, On-Device AI 모델 캐시처럼 로그인 없이 사용할 수 있는 비민감 정보만 저장한다.

### 3.3 요약 길이의 구분

- 수집·공개 데이터의 `summary`는 최대 150자다. `collector.py`, `export_static.py`, `validate_data.py`의 제한을 함께 유지한다.
- On-Device AI의 생성 결과에는 150자 제한을 적용하지 않는다. 수집 데이터 제한과 AI 출력 제한을 혼동하지 않는다.

## 4. 수집 운영 규칙

현재 출처와 표시 순서는 다음과 같다.

| key | 출처 | 엔진 |
|---|---|---|
| `univ` | 상명대학교 공지사항 | `smart` |
| `coneng` | 융합공과대학 공지사항 | `smart` |
| `fbs` | 핀빅스 공지사항 | `smart` |
| `recruit` | 핀빅스 채용·홍보 | `smart` |
| `cs_recruit` | 컴퓨터과학전공 채용·홍보 | `smart` |
| `calendar` | 학사일정 및 전공일정 | `calendar` |
| `restaurant` | 학식 | `restaurant` |
| `today` | 상명투데이 | `webzine` |
| `newsletter` | 상명 뉴스레터 | `webzine` |
| `people` | 상명피플 | `webzine` |
| `focus` | 언론 속 상명 | `webzine` |

- 수집은 증분 방식이 기본이며 전량 재수집을 반복하지 않는다.
- 요청 사이의 최소 간격, 지터, 제한된 재시도, `Retry-After` 처리를 유지한다. 현재 기본값은 `THROTTLE=1.0`, `RETRIES=3`이다.
- User-Agent에 비공식 프로젝트임과 GitHub Issues 문의 경로를 표시한다.
- 새 호스트를 추가하기 전에 `robots.txt`와 이용 조건을 확인한다.
- `lib.smu.ac.kr`은 `robots.txt`가 `Disallow: /`이므로 명시적 허락 없이 수집 대상에 추가하지 않는다.
- 인스타그램은 콘텐츠를 수집하거나 임베드하지 않고 사용자가 제공한 공개 프로필 링크만 정적으로 제공한다.
- 오늘 날짜, 학사일정, 학식 조회일, 수집·갱신 시각은 실행 기기의 시간대와 관계없이 `Asia/Seoul`로 계산한다.
- 자동 수집은 GitHub Actions에서 매일 한국 시간 06:10과 15:10에 실행한다. 출처 하나라도 실패하면 새 공개 데이터를 내보내지 않는다.

### 파서 변경 시 주의사항

- `smart` 게시판은 `article.offset`, `articleLimit`, `srCategoryId1`을 사용한다. 상세 본문은 호출하지 않는다.
- `webzine` 4종은 마크업이 서로 다르다. `today`, `newsletter`, `people`, `focus`를 모두 회귀 검증한다.
- `newsletter`는 목록 메타데이터만 사용한다.
- `people`의 작성자와 작성일은 CSS 클래스가 겹치므로 숨김 라벨로 구분한다.
- `focus`는 외부 URL을 기본키로 사용하며 잘못된 URL은 `link_type="broken"`으로 유지한다.
- `calendar`는 `selectCalendarArticle` JSON API에 연도를 반드시 전달한다.
- `restaurant`는 `smu-table tb-w150` 범위에서 파싱하고 한식과 푸드코트의 `corner`, `menu` 구조를 보존한다.
- `recruit`와 `cs_recruit`는 화면의 `채용·홍보` 한 탭에서 함께 표시하되 출처 태그와 출처 필터로 구분한다. 두 게시판에 같은 공고가 올라와도 자동으로 병합하거나 삭제하지 않는다.
- 파서를 변경하면 11개 출처 전체 테스트와 공개 데이터 검증을 수행한다.

## 5. 프론트엔드 규칙

### 5.1 반응형 UI와 접근성

- 780px 이하에서는 모바일 상단 영역을 사용한다. 하단 고정 탭을 만들지 않으며, 상단 메뉴 버튼으로 좌측 메뉴를 연다.
- 781px 이상 1050px 이하에서는 `.side-rail`을 `display: contents`로 두어 AI 카드, 본문, 다가오는 일정, 인스타그램 카드를 본문과 같은 너비의 한 줄 세로 배치로 쌓는다. 이 구간에 우측 레일용 2열 그리드나 카드 사이를 채우는 굵은 색 띠를 다시 넣지 않는다.
- 상단 바의 `.topbar-inner`는 모든 폭에서 `.app-shell`과 같은 너비와 좌우 기준선을 사용한다. 781px 이상 1180px 이하에서는 브랜드와 상단 메뉴 글자를 `clamp()`로 데스크톱 크기까지 이어서 키우고, 특정 구간만 고정된 작은 값으로 되돌리지 않는다.
- 모바일 검색은 상단의 검색 아이콘으로 열고 닫는다. 연락처 검색 탭에서는 별도의 공식 연락처 검색 폼을 사용하므로 일반 검색 아이콘을 숨긴다.
- 모바일 메뉴, 검색, AI 패널을 열었을 때 포커스 이동, `aria-expanded`, `aria-hidden`, `inert`와 닫기 동작을 함께 유지한다.
- 주요 본문 글자는 16px 수준을 기준으로 하고, 버튼과 입력 요소는 충분한 터치 영역을 확보한다.
- 키보드 포커스, 의미 있는 `aria-label`, 상태 알림의 `aria-live`, 색상 대비를 유지한다.
- 일반 UI에는 고정폭 글꼴을 사용하지 않는다. 시스템 고정폭 글꼴은 `code` 요소에만 사용한다.
- 사이트는 라이트 테마만 사용한다. 다크 테마 선택 UI와 `prefers-color-scheme: dark` 기반 색상 전환을 다시 추가하지 않는다.
- 상단 바와 푸터 사이의 전체 콘텐츠 영역에 배경 그라데이션을 유지한다. 포인터 이동에 따른 밝기 효과는 콘텐츠 가독성을 해치지 않아야 하며, 터치 환경과 움직임 줄이기 설정에서는 안정적으로 동작해야 한다.
- AI 패널의 외곽은 모든 화면에서 둥근 모서리를 유지하고, 헤더가 아니라 내용 영역만 내부 스크롤한다. 패널과 런처가 푸터의 이용약관, 개인정보 처리방침, 설정 등의 조작을 가리지 않게 한다.
- `prefers-reduced-motion`과 설정의 움직임 줄이기를 존중한다.

### 5.2 문서 페이지

- `pages/`의 이용약관, 개인정보 처리방침, 저작권 정책, 라이선스와 문의 문서는 같은 타이포그래피와 카드 체계를 사용한다.
- 문서 제목 위에 `PRIVACY POLICY` 같은 원형 영문 표식을 표시하지 않는다.
- 시행일 아래에 `작성 기준`이나 근거 법령을 별도 메타데이터 행으로 표시하지 않는다.
- 안내문과 현재 상태에 좌측 세로형 강조 막대를 사용하지 않는다.
- 법령명과 조문은 실제 문서 내용에 필요한 경우에만 본문에서 설명한다. 장식적이거나 중복된 작성 근거 문구를 추가하지 않는다.

### 5.3 상태와 보안

- 탭 이동 시 검색어, 표시 개수, AI 대화 상태처럼 해당 탭에 종속된 상태를 올바르게 초기화한다.
- 사용자 입력, 수집 데이터, AI 출력은 HTML에 넣기 전에 이스케이프하거나 허용된 Markdown만 안전하게 렌더링한다.
- URL은 `http:`와 `https:`만 허용하고 외부 새 창 링크에는 `rel="noopener noreferrer"`를 사용한다.
- `innerHTML`을 사용할 때 수집 데이터나 사용자 입력을 그대로 보간하지 않는다.
- 즐겨찾기, 읽음, 관심 정보는 로그인 없이 브라우저에만 저장한다.
- 공지사항, 채용·홍보, 웹진의 항목 공유에는 제목과 표시 메타데이터, 원문 URL을 포함한다.
- 학사일정은 일정명과 날짜만 공유하고, 모든 일정에 반복되는 공통 원문 URL은 포함하지 않는다.
- 학식은 선택한 날짜의 코너별 메뉴만 공유하고, 모든 식단에 반복되는 공통 원문 URL은 포함하지 않는다.
- 클립보드 공유 대체 경로에서는 제목이 포함된 본문 앞에 공유 제목을 다시 붙이지 않는다.
- 링크가 필요한 공유는 본문 뒤에 빈 줄과 원문 URL을 넣은 단일 텍스트로 전달하며 Web Share API의 `url` 필드를 별도로 사용하지 않는다.

### 5.4 On-Device AI

- On-Device AI 모델은 사용자가 설정에서 명시적으로 다운로드하기 전까지 자동으로 받지 않는다.
- 모델이 준비되지 않아도 공지, 학사일정, 학식, 웹진 등 기본 기능은 정상 동작해야 한다.
- 추론은 브라우저의 WebGPU와 모델 캐시를 사용하며 입력을 외부 AI 서버로 전송하는 폴백을 추가하지 않는다.
- 공지사항, 채용·홍보, 학사일정, 학식, 웹진은 각각의 데이터 구조에 맞는 시스템 프롬프트를 유지한다.
- 연락처 검색 탭에서도 AI 메뉴에 접근할 수 있어야 한다. 다만 공식 상명대학교 페이지에서 열린 검색 결과를 가져오거나 분석한다고 표현하지 않는다.
- AI 근거 데이터는 현재 공개 목록에서 최대 12건, 7,200자까지 선별한다. 제목 중심 자료와 짧은 발췌 포함 자료를 구분하고, 제목만으로 기간, 자격, 금액, 장소와 방법을 추론하지 않는다.
- 사실 중심 요약은 `do_sample: false`를 유지한다. 샘플링을 다시 켜지 않는 한 효력이 없는 `temperature` 값을 추가하지 않는다.
- AI 응답은 Markdown 원문을 그대로 노출하지 않고 렌더링된 결과를 스트리밍한다.
- 생성 중에는 스트리밍 영역의 커서가 깜빡여야 하며, 움직임 줄이기 환경에서는 애니메이션을 비활성화한다.
- `생성형 AI가 만든 내용입니다.` 고지에는 정보 아이콘을 함께 표시한다.
- AI 런처와 패널에는 같은 프로세서 모양 아이콘을 사용한다. 모델이 없으면 무표정, 모델이 준비되면 웃는 얼굴 상태를 모든 런처에 일관되게 반영한다.
- 탭 이동 시 이전 탭의 AI 요약과 후속 질문 문맥을 초기화한다.
- 음성 재생, 외부 공유, 생성 중지, 후속 질문 기능이 함께 동작하는지 확인한다.
- AI 공유 버튼은 목록에서 사용하는 공유 아이콘과 시각 언어를 맞춘다.
- AI 외부 공유에는 생성된 답변 본문만 포함하고 현재 페이지 URL을 첨부하지 않는다.

## 6. 캐시와 배포

- `app.js`나 `styles.css`를 변경하면 `index.html`의 쿼리 버전을 갱신한다.
- `pages/page.css`를 변경하면 이를 불러오는 모든 문서의 쿼리 버전을 함께 갱신한다.
- 앱 셸 파일이나 공개 데이터 캐시 정책을 변경하면 `sw.js`의 `CACHE_NAME`을 증가시킨다.
- 서비스 워커는 온라인에서 최신 응답을 우선하고 실패 시 캐시를 사용한다. 오프라인 폴백을 깨뜨리지 않는다.
- GitHub Pages에는 `build_site.py`가 만든 `dist/`만 배포한다.
- 법적 고지 페이지나 정적 자산을 추가하면 `build_site.py`의 공개 목록과 링크를 함께 확인한다.

## 7. 검증 절차

변경 범위에 맞는 최소 검증을 수행하고, 배포 전에는 전체 검증을 수행한다.

```bash
# Python 파서와 공개 데이터 정책
python3 -m unittest discover -s tests
python3 validate_data.py

# On-Device AI
node --test tests/test_local_llm.mjs

# JavaScript 문법과 공개 산출물
node --check app.js
node --check local-llm.js
python3 build_site.py
python3 verify_deploy.py
```

- 수집 로직을 변경했을 때만 필요한 범위에서 `python3 ingest.py --board <key> --limit 30`을 실행한다.
- 공개 데이터를 갱신하면 `python3 export_static.py` 후 `python3 validate_data.py`를 실행한다.
- UI 변경은 최소한 데스크톱과 390px 내외 모바일 화면에서 확인한다.
- 내비게이션, 검색, 대화상자, AI 패널과 움직임 관련 변경은 실제 클릭과 전환 동작으로 검증한다.
- 작업을 완료하기 전에 `dist/`가 원본과 동기화되었는지 확인한다.
